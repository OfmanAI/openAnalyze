# --- Core CLI & analytics ---
pandas>=1.5
scipy>=1.10
scikit-learn>=1.4
rich>=13.7

# --- GUI ----
ttkbootstrap>=1.10
pillow>=10.2
plotly>=6.0      # interactive confusion matrix

# --- Extras the GUI auto-installs on first run ---
# (openAnalyze already shells out to R; no PyPI pin needed here)

